# Core
requests>=2.28.0
Pillow>=9.0.0
certifi>=2023.7.22

# Reads .env for local development. Optional — on the Pi, systemd supplies the
# same variables via EnvironmentFile.
python-dotenv>=1.0.0

# GTFS-realtime feed parsing
protobuf>=3.20.0
gtfs-realtime-bindings>=1.0.0

# Timezone database. Bundled on most Linux distros; needed on Windows and on
# slim containers, otherwise the clock falls back to system local time.
tzdata>=2023.3; sys_platform == "win32"

# LED matrix driver — NOT installable from PyPI. Build it on the Pi:
#   git clone https://github.com/hzeller/rpi-rgb-led-matrix.git
#   cd rpi-rgb-led-matrix && make build-python PYTHON=$(which python3)
#   sudo make install-python PYTHON=$(which python3)
# The app runs without it in MATRIX_LIBRARY=test mode, rendering to PNG files.
